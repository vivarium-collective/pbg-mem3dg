"""Custom bigraph-schema types for Mem3DG wrapper."""


def register_mem3dg_types(core):
    """Register custom types used by Mem3DG processes.

    Currently uses built-in types only. This function is a hook for
    future custom type registrations (e.g. mesh types, unit-bearing
    quantities).
    """
    pass
